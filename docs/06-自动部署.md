# 06-自动部署

生产自动部署只从 `main` 触发。

## GitHub Actions 步骤

1. `npm ci`
2. `npm run lint`
3. `npm run build`
4. 验证 `out/index.html` 和 `out/_next/`
5. 安装 `bcecmd`
6. 执行 `scripts/deploy.sh`
7. 上传到 Baidu BOS

## 回滚

当前静态部署会替换 BOS 目标路径内容。需要回滚时，建议重新运行一个已知稳定提交对应的 GitHub Actions workflow，或从备份/CDN 版本中恢复对象。
